#include <stdio.h>
{
	int a[100],i,loc,mid,beg,end,n;
	flag=0,item;
	clrscr;
	printf("how many  elements");
	scanf("%d",&n);
	printf("enter the element of the array");

	for (i=0;i<n-1;i++)
	{
		scanf("%d",&a[i]);
	}
	printf ("enter the element to be searched")
	scanf("%d",&item);
	loc=0;
	beg=0;
	end=n-1;
	while ((beg <= end) && ( item != a[mid]))
	{
		mid = ((beg+end)/2);
		if (item==a[mid])
	{
		printf("search is successfull")

		loc= mid;
		printf ("position of item %d",loc+1);
		flag=flag+1;

	}
	else if (item < a[mid])
